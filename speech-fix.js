// Pronunciation layer for headwords and conjugated forms.
// Bundled MP3 -> streamed Persian TTS -> installed Persian voice -> phonetic English fallback.
(()=>{function phoneticEnglish(text){return String(text||'').toLowerCase().replaceAll('â','aa').replaceAll('ā','aa').replaceAll('kh','k h').replaceAll('gh','g h').replaceAll('zh','j').replaceAll('q','g').replaceAll('-',' ').replace(/[?!.,]/g,' ').replace(/\s+/g,' ').trim()}function chooseEnglishVoice(){if(!('speechSynthesis'in window))return null;const v=window.speechSynthesis.getVoices()||[];return v.find(x=>/^en-US$/i.test(x.lang))||v.find(x=>/^en(?:-|_|$)/i.test(x.lang))||v[0]||null}function playDiagnosticTone(requestId,button){if(requestId!==speechRequest)return;setSpeechButtonBusy(button,false);try{const C=window.AudioContext||window.webkitAudioContext;if(!C)throw new Error();const c=new C(),o=c.createOscillator(),g=c.createGain();o.frequency.value=523.25;g.gain.setValueAtTime(.0001,c.currentTime);g.gain.exponentialRampToValueAtTime(.12,c.currentTime+.02);g.gain.exponentialRampToValueAtTime(.0001,c.currentTime+.22);o.connect(g);g.connect(c.destination);o.start();o.stop(c.currentTime+.24);o.addEventListener('ended',()=>c.close());toast('Speech was blocked. Check that this tab is not muted.')}catch{toast('Audio is blocked. Check the tab mute icon and your sound output.')}}function speakPhonetic(word,requestId,button){if(!('speechSynthesis'in window)||requestId!==speechRequest){playDiagnosticTone(requestId,button);return}const text=phoneticEnglish(word?.latin||'');if(!text){playDiagnosticTone(requestId,button);return}const s=window.speechSynthesis,u=new SpeechSynthesisUtterance(text),v=chooseEnglishVoice();let started=false,finished=false;if(v)u.voice=v;u.lang=v?.lang||'en-US';u.rate=.72;u.pitch=1;u.volume=1;u.onstart=()=>{if(requestId!==speechRequest)return;started=true;setSpeechButtonBusy(button,false)};u.onend=()=>{if(requestId!==speechRequest)return;finished=true;activeUtterance=null;setSpeechButtonBusy(button,false)};u.onerror=()=>{if(requestId!==speechRequest||finished)return;finished=true;activeUtterance=null;playDiagnosticTone(requestId,button)};activeUtterance=u;s.cancel();s.resume();s.speak(u);setTimeout(()=>{if(requestId!==speechRequest||started||finished)return;s.cancel();playDiagnosticTone(requestId,button)},2800)}function speakWithPersianFallback(text,word,requestId,button){if(requestId!==speechRequest)return;const v=typeof findPersianVoice==='function'?findPersianVoice():null;if(v&&'speechSynthesis'in window){const u=new SpeechSynthesisUtterance(text);u.voice=v;u.lang=v.lang||'fa-IR';u.rate=.78;u.volume=1;u.onstart=()=>setSpeechButtonBusy(button,false);u.onend=()=>{activeUtterance=null;setSpeechButtonBusy(button,false)};u.onerror=()=>speakPhonetic(word,requestId,button);activeUtterance=u;window.speechSynthesis.cancel();window.speechSynthesis.resume();window.speechSynthesis.speak(u);return}speakPhonetic(word,requestId,button)}function playStreamedPersian(text,word,requestId,button){if(requestId!==speechRequest)return;const encoded=encodeURIComponent(text),audio=document.createElement('audio');audio.preload='auto';audio.playsInline=true;audio.volume=1;[`https://translate.google.com/translate_tts?ie=UTF-8&client=tw-ob&tl=fa&ttsspeed=0.8&q=${encoded}`,`https://translate.googleapis.com/translate_tts?ie=UTF-8&client=gtx&tl=fa&ttsspeed=0.8&q=${encoded}`].forEach(url=>{const source=document.createElement('source');source.src=url;source.type='audio/mpeg';audio.appendChild(source)});activeAudio=audio;let started=false,failed=false;const fail=()=>{if(failed||started||requestId!==speechRequest)return;failed=true;if(activeAudio===audio)activeAudio=null;audio.pause();speakWithPersianFallback(text,word,requestId,button)};audio.addEventListener('playing',()=>{if(requestId!==speechRequest)return;started=true;setSpeechButtonBusy(button,false)},{once:true});audio.addEventListener('ended',()=>{if(requestId!==speechRequest)return;if(activeAudio===audio)activeAudio=null;setSpeechButtonBusy(button,false)},{once:true});audio.addEventListener('error',fail,{once:true});audio.play().catch(fail);setTimeout(fail,2500)}speak=function(text,button=null){stopSpeech();const requestId=speechRequest;setSpeechButtonBusy(button,true);const wordIndex=Array.isArray(WORDS)?WORDS.findIndex(w=>w.fa===text):-1,word=wordIndex>=0?WORDS[wordIndex]:{fa:text,latin:''};if(wordIndex<0){playStreamedPersian(text,word,requestId,button);return}const filename=String(wordIndex).padStart(3,'0'),audio=new Audio(`audio/${filename}.mp3?v=5`);audio.preload='auto';audio.playsInline=true;audio.volume=1;activeAudio=audio;let started=false,failed=false;const fail=()=>{if(failed||started||requestId!==speechRequest)return;failed=true;if(activeAudio===audio)activeAudio=null;audio.pause();playStreamedPersian(text,word,requestId,button)};audio.addEventListener('playing',()=>{if(requestId!==speechRequest)return;started=true;setSpeechButtonBusy(button,false)},{once:true});audio.addEventListener('ended',()=>{if(requestId!==speechRequest)return;if(activeAudio===audio)activeAudio=null;setSpeechButtonBusy(button,false)},{once:true});audio.addEventListener('error',fail,{once:true});audio.play().catch(fail);setTimeout(fail,1100)}})();
